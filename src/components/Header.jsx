import React from 'react';
import './Header.css';

const Header = () => {
  return (
    <header>
      <span className="header__title">Albumify</span>
    </header>
  );
};

export default Header;
